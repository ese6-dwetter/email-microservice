apiVersion: v1
kind: Service
metadata:
  name: email-microservice
  namespace: dwetter-develop
spec:
  selector:
    app: email-microservice
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
